function dots
	cd $XDG_PROJECTS_DIR/dotfiles && git status
end

function glazy
	git add . && git commit -m $argv && git push
end

function lz
	eza -l -a
end

function ce
	cd $XDG_CONFIG_HOME/$argv
	nvim .
end
